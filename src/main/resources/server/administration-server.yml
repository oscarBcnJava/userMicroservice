spring:
  application:
    name: administration-server


error:
  path: /error

server:
  port: 2001

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/
  instance:
    leaseRenewalIntervalInSeconds: 5

