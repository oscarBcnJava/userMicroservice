spring:
  application:
    name: search-server


error:
  path: /error

server:
  port: 2003

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/
  instance:
    leaseRenewalIntervalInSeconds: 5

